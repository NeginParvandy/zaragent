# سازگاری Python

این Release آفلاین فقط برای **Windows 64-bit و Python 3.13** بسته‌بندی شده است.

فایل‌های داخل `wheelhouse` شامل Wheelهای `cp313-win_amd64` هستند. بنابراین برای نصب روی نسخه یا معماری دیگر باید Wheelhouse جداگانه ساخته شود.

نصب:

```cmd
install.bat
```
